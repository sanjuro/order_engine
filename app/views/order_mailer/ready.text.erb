Your order is ready please come and collect at <%= @order.store.store_name %>.

<% if @order.store_order_number.nil? %>
	Your order number is <%= @order.number %>
<% else %>
	Your order number is <%= @order.store_order_number %>
<% end %>

Order Summary
<% @order.line_items.each do |item| %>
	<%= item.variant.product.name %>
	<%= item.quantity %> X <%= item.price %>
	total: <%= (item.price * item.quantity) %>
<% end %>

<%= t(:subtotal) %>:
<%= @order.item_total %>

<%= t(:order_total) %>:
<%= @order.total %>

Thanks for ordering with Vosto.