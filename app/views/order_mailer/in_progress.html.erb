<!DOCTYPE html>
<html>
  <head>
    <meta content="text/html; charset=UTF-8" http-equiv="Content-Type" />
  </head>
  <body>
    <h1>Your order is being cooked up at <%= @order.store.store_name %> and will be ready in <%= @order.time_to_ready %> minutes.</h1>
    <p>
    <% if @order.store_order_number.nil? %>
      Your order number is <%= @order.number %><br/>
    <% else %>
      Your order number is <%= @order.store_order_number %><br/>
    <% end %>   
    </p>
    <h3> Order Summary </h3>
    <% @order.line_items.each do |item| %>
    <div class="cart_item">
      <div class="cart-item-description">
        <h2><%= item.variant.product.name %></h2>
      </div>
      <div class="cart-item-quantity">
        <%= item.quantity %> X <%= item.price %>
      </div>
      <div class="cart-item-total">
        total: <%= (item.price * item.quantity) %>
      </div> 
      <br>
    </div>
    <% end %>


    <div id="cart-summary">

      <div id="subtotal" class="cart-summary-item">
        <b><%= t(:subtotal) %>:</b> 
        <span><%= @order.item_total %></span>
      </div>

      <div id="order_total" class="cart-summary-item">
        <b><%= t(:order_total) %>:</b> 
        <span><%= @order.total %></span>
      </div>
      
    </div>
    <p>Thanks for ordering with Vosto.</p>
  </body>
</html>